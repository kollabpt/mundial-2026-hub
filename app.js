const matchGrid=document.querySelector('#matchGrid'),cityGrid=document.querySelector('#cityGrid'),teamGrid=document.querySelector('#teamGrid'),storyGrid=document.querySelector('#storyGrid'),teamSearch=document.querySelector('#teamSearch');function renderMatches(){matchGrid.innerHTML=worldCupGuide.matches.map(m=>`<article class="match-card"><div class="card-top"><span>${m.date}</span><strong>${m.tag}</strong></div><h3>${m.title}</h3><p class="place">${m.place}</p><p>${m.note}</p></article>`).join('')}function renderCities(){cityGrid.innerHTML=worldCupGuide.cities.map(([city,country,note],i)=>`<article class="city-card city-card--${i%4}"><span>${country}</span><h3>${city}</h3><p>${note}</p></article>`).join('')}function renderTeams(){const q=teamSearch.value.trim().toLowerCase();teamGrid.innerHTML=worldCupGuide.teams.filter(t=>[t.code,t.name,t.confederation].join(' ').toLowerCase().includes(q)).map(t=>`<article class="team-card"><div class="team-head"><span class="badge">${t.code}</span><div><h3>${t.name}</h3><small>${t.confederation}</small></div></div><p>${t.profile}</p><div class="chips">${t.watch.map(p=>`<span>${p}</span>`).join('')}</div></article>`).join('')}function renderStories(){storyGrid.innerHTML=worldCupGuide.stories.map(([title,note])=>`<article><h3>${title}</h3><p>${note}</p></article>`).join('')}teamSearch.addEventListener('input',renderTeams);renderMatches();renderCities();renderTeams();renderStories();
